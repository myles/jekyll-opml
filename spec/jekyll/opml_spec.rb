require 'spec_helper'

describe Jekyll::Opml do
  it 'has a version number' do
    expect(Jekyll::Opml::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
